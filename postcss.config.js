module.exports = {
  plugins: ['tailwindcss', 'postcss-nesting', 'autoprefixer'],
};

// just for testing!
// -use this with build script build:css
// module.exports = {
//   plugins: [require('tailwindcss'), require('postcss-nesting'), require('autoprefixer')],
// };
